<?xml version="1.0"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

    {# Ссылки на бренды #}
    <sitemap>
        <loc>{{ absolute_url(path('reference-cars:sitemap.brands.urls')) }}</loc>
    </sitemap>

    {# Карат на модели брендов #}
    <sitemap>
        <loc>{{ absolute_url(path('reference-cars:sitemap.brands')) }}</loc>
    </sitemap>

</sitemapindex>