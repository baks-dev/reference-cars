{% trans_default_domain 'admin.reference.cars.brand' %}



<div class="d-flex justify-content-between align-items-center gap-3">

    <div class="w-50">
        {{ include(_self|replace({ "form.html.twig": "_logo.html.twig" })) }}
    </div>

    <div class="w-50">
        {{ include(_self|replace({ "form.html.twig": "_trans.html.twig" })) }}
        {#{{ form_row(form.counter, { label : 'Количество стикеров' }) }}#}

        {{ form_row(form.info.url, { label : 'Семантическая ссылка' }) }}
    </div>



</div>

